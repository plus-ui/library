---
title: Popover
description: Displays informative content in a floating panel near a target element.
---

import { Tabs, TabItem, Aside } from '@astrojs/starlight/components';
import { EmbedContent } from '../../../component';
import { Image } from 'astro:assets';

import anatomy from '../../../assets/components/popover/anatomy.png';
import layoutSpacing from '../../../assets/components/popover/layout-spacing.png';
import lightDarkMode from '../../../assets/components/popover/light-dark-mode.png';

The Popover component is used to display additional information, options, or actions in a floating panel that appears near a trigger element. It's useful for tooltips with rich content, dropdown menus, or contextual information displays.

## Anatomy

<Image src={anatomy} alt="Popover - anatomy" />

## Usage

### Basic Example

A basic popover triggered by clicking a button, displaying simple text content.

<code-bubble class="not-content">

```html
<plus-popover header-text="Information" text="This is the popover content.">
  <plus-button>Show Popover</plus-button>
</plus-popover>
```

```tsx
import { PlusPopover, PlusButton } from '@plusui/library/react';

export default () => {
  return (
    <PlusPopover headerText="Information" text="This is the popover content.">
      <PlusButton>Show Popover</PlusButton>
    </PlusPopover>
  );
};
```

```ts
import { Component } from '@angular/core';

@Component({
  selector: 'app-popover-basic',
  template: `
    <plus-popover header-text="Information" text="This is the popover content.">
      <plus-button>Show Popover</plus-button>
    </plus-popover>
  `
})
export class PopoverBasicComponent {}
```

```vue
<template>
  <plus-popover header-text="Information" text="This is the popover content.">
    <plus-button>Show Popover</plus-button>
  </plus-popover>
</template>

<script setup>
// Assuming Plus UI components are registered globally or imported locally
</script>
```
</code-bubble>

### Orientation

Use the `orientation` prop to control where the popover appears relative to the trigger element.

<code-bubble class="not-content horizontal">

```html
<plus-popover orientation="top" header-text="Top">
  <plus-button>Top</plus-button>
</plus-popover>
<plus-popover orientation="bottom" header-text="Bottom">
  <plus-button>Bottom</plus-button>
</plus-popover>
<plus-popover orientation="left" header-text="Left">
  <plus-button>Left</plus-button>
</plus-popover>
<plus-popover orientation="right" header-text="Right">
  <plus-button>Right</plus-button>
</plus-popover>
<plus-popover orientation="top-start" header-text="Top Start">
  <plus-button>Top Start</plus-button>
</plus-popover>
<plus-popover orientation="bottom-end" header-text="Bottom End">
  <plus-button>Bottom End</plus-button>
</plus-popover>
```

```tsx
import { PlusPopover, PlusButton } from '@plusui/library/react';

export default () => {
  return (
    <>
      <PlusPopover orientation="top" headerText="Top"><PlusButton>Top</PlusButton></PlusPopover>
      <PlusPopover orientation="bottom" headerText="Bottom"><PlusButton>Bottom</PlusButton></PlusPopover>
      <PlusPopover orientation="left" headerText="Left"><PlusButton>Left</PlusButton></PlusPopover>
      <PlusPopover orientation="right" headerText="Right"><PlusButton>Right</PlusButton></PlusPopover>
      <PlusPopover orientation="top-start" headerText="Top Start"><PlusButton>Top Start</PlusButton></PlusPopover>
      <PlusPopover orientation="bottom-end" headerText="Bottom End"><PlusButton>Bottom End</PlusButton></PlusPopover>
    </>
  );
};
```

```ts
import { Component } from '@angular/core';

@Component({
  selector: 'app-popover-orientation',
  template: `
    <plus-popover orientation="top" header-text="Top"><plus-button>Top</plus-button></plus-popover>
    <plus-popover orientation="bottom" header-text="Bottom"><plus-button>Bottom</plus-button></plus-popover>
    <plus-popover orientation="left" header-text="Left"><plus-button>Left</plus-button></plus-popover>
    <plus-popover orientation="right" header-text="Right"><plus-button>Right</plus-button></plus-popover>
    <plus-popover orientation="top-start" header-text="Top Start"><plus-button>Top Start</plus-button></plus-popover>
    <plus-popover orientation="bottom-end" header-text="Bottom End"><plus-button>Bottom End</plus-button></plus-popover>
  `
})
export class PopoverOrientationComponent {}
```

```vue
<template>
  <plus-popover orientation="top" header-text="Top"><plus-button>Top</plus-button></plus-popover>
  <plus-popover orientation="bottom" header-text="Bottom"><plus-button>Bottom</plus-button></plus-popover>
  <plus-popover orientation="left" header-text="Left"><plus-button>Left</plus-button></plus-popover>
  <plus-popover orientation="right" header-text="Right"><plus-button>Right</plus-button></plus-popover>
  <plus-popover orientation="top-start" header-text="Top Start"><plus-button>Top Start</plus-button></plus-popover>
  <plus-popover orientation="bottom-end" header-text="Bottom End"><plus-button>Bottom End</plus-button></plus-popover>
</template>
```
</code-bubble>

### Trigger

The `trigger` prop determines how the popover is opened.

<code-bubble class="not-content horizontal">

```html
<plus-popover trigger="click" header-text="Click Me">
  <plus-button>Click Trigger (Default)</plus-button>
</plus-popover>
<plus-popover trigger="hover" header-text="Hover Over Me">
  <plus-button>Hover Trigger</plus-button>
</plus-popover>
```

```tsx
import { PlusPopover, PlusButton } from '@plusui/library/react';

export default () => {
  return (
    <>
      <PlusPopover trigger="click" headerText="Click Me">
        <PlusButton>Click Trigger (Default)</PlusButton>
      </PlusPopover>
      <PlusPopover trigger="hover" headerText="Hover Over Me">
        <PlusButton>Hover Trigger</PlusButton>
      </PlusPopover>
    </>
  );
};
```

```ts
import { Component } from '@angular/core';

@Component({
  selector: 'app-popover-trigger',
  template: `
    <plus-popover trigger="click" header-text="Click Me">
      <plus-button>Click Trigger (Default)</plus-button>
    </plus-popover>
    <plus-popover trigger="hover" header-text="Hover Over Me">
      <plus-button>Hover Trigger</plus-button>
    </plus-popover>
  `
})
export class PopoverTriggerComponent {}
```

```vue
<template>
  <plus-popover trigger="click" header-text="Click Me">
    <plus-button>Click Trigger (Default)</plus-button>
  </plus-popover>
  <plus-popover trigger="hover" header-text="Hover Over Me">
    <plus-button>Hover Trigger</plus-button>
  </plus-popover>
</template>
```
</code-bubble>

### Status

Use the `status` prop to apply different color schemes and default icons.

<code-bubble class="not-content horizontal">

```html
<plus-popover status="default" header-text="Default">
  <plus-button>Default</plus-button>
</plus-popover>
<plus-popover status="primary" header-text="Primary">
  <plus-button status="primary">Primary</plus-button>
</plus-popover>
<plus-popover status="success" header-text="Success">
  <plus-button status="success">Success</plus-button>
</plus-popover>
<plus-popover status="warning" header-text="Warning">
  <plus-button status="warning">Warning</plus-button>
</plus-popover>
<plus-popover status="danger" header-text="Danger">
  <plus-button status="danger">Danger</plus-button>
</plus-popover>
<plus-popover status="info" header-text="Info">
  <plus-button status="info">Info</plus-button>
</plus-popover>
```

```tsx
import { PlusPopover, PlusButton } from '@plusui/library/react';

export default () => {
  return (
    <>
      <PlusPopover status="default" headerText="Default"><PlusButton>Default</PlusButton></PlusPopover>
      <PlusPopover status="primary" headerText="Primary"><PlusButton status="primary">Primary</PlusButton></PlusPopover>
      <PlusPopover status="success" headerText="Success"><PlusButton status="success">Success</PlusButton></PlusPopover>
      <PlusPopover status="warning" headerText="Warning"><PlusButton status="warning">Warning</PlusButton></PlusPopover>
      <PlusPopover status="danger" headerText="Danger"><PlusButton status="danger">Danger</PlusButton></PlusPopover>
      <PlusPopover status="info" headerText="Info"><PlusButton status="info">Info</PlusButton></PlusPopover>
    </>
  );
};
```

```ts
import { Component } from '@angular/core';

@Component({
  selector: 'app-popover-status',
  template: `
    <plus-popover status="default" header-text="Default"><plus-button>Default</plus-button></plus-popover>
    <plus-popover status="primary" header-text="Primary"><plus-button status="primary">Primary</plus-button></plus-popover>
    <plus-popover status="success" header-text="Success"><plus-button status="success">Success</plus-button></plus-popover>
    <plus-popover status="warning" header-text="Warning"><plus-button status="warning">Warning</plus-button></plus-popover>
    <plus-popover status="danger" header-text="Danger"><plus-button status="danger">Danger</plus-button></plus-popover>
    <plus-popover status="info" header-text="Info"><plus-button status="info">Info</plus-button></plus-popover>
  `
})
export class PopoverStatusComponent {}
```

```vue
<template>
  <plus-popover status="default" header-text="Default"><plus-button>Default</plus-button></plus-popover>
  <plus-popover status="primary" header-text="Primary"><plus-button status="primary">Primary</plus-button></plus-popover>
  <plus-popover status="success" header-text="Success"><plus-button status="success">Success</plus-button></plus-popover>
  <plus-popover status="warning" header-text="Warning"><plus-button status="warning">Warning</plus-button></plus-popover>
  <plus-popover status="danger" header-text="Danger"><plus-button status="danger">Danger</plus-button></plus-popover>
  <plus-popover status="info" header-text="Info"><plus-button status="info">Info</plus-button></plus-popover>
</template>
```
</code-bubble>

### Custom Content and Title

Use slots `title` and `content` for more complex content instead of the `header-text` and `text` props.

<code-bubble class="not-content">

```html
<plus-popover>
  <span slot="title">
    <i class="fa-solid fa-star" style="color: gold;"></i>
    Custom Title Slot
  </span>
  <div slot="content">
    This is <strong>custom popover</strong> content with <em>HTML</em> placed in the content slot.
    <plus-divider class="my-2"></plus-divider>
    <plus-button size="sm">Action</plus-button>
  </div>
  <plus-button>Show Custom Popover</plus-button>
</plus-popover>
```

```tsx
import { PlusPopover, PlusButton, PlusDivider } from '@plusui/library/react';

export default () => {
  return (
    <PlusPopover>
      <span slot="title">
        <i className="fa-solid fa-star" style={{ color: 'gold' }}></i>
        Custom Title Slot
      </span>
      <div slot="content">
        This is <strong>custom popover</strong> content with <em>HTML</em> placed in the content slot.
        <PlusDivider className="my-2"></PlusDivider>
        <PlusButton size="sm">Action</PlusButton>
      </div>
      <PlusButton>Show Custom Popover</PlusButton>
    </PlusPopover>
  );
};
```

```ts
import { Component } from '@angular/core';

@Component({
  selector: 'app-popover-custom',
  template: `
    <plus-popover>
      <span slot="title">
        <i class="fa-solid fa-star" style="color: gold;"></i>
        Custom Title Slot
      </span>
      <div slot="content">
        This is <strong>custom popover</strong> content with <em>HTML</em> placed in the content slot.
        <plus-divider class="my-2"></plus-divider>
        <plus-button size="sm">Action</plus-button>
      </div>
      <plus-button>Show Custom Popover</plus-button>
    </plus-popover>
  `
})
export class PopoverCustomComponent {}
```

```vue
<template>
  <plus-popover>
    <span slot="title">
      <i class="fa-solid fa-star" style="color: gold;"></i>
      Custom Title Slot
    </span>
    <div slot="content">
      This is <strong>custom popover</strong> content with <em>HTML</em> placed in the content slot.
      <plus-divider class="my-2"></plus-divider>
      <plus-button size="sm">Action</plus-button>
    </div>
    <plus-button>Show Custom Popover</plus-button>
  </plus-popover>
</template>
```
</code-bubble>

### Dismissable

Set `dismissable="false"` to hide the default close button.

<code-bubble class="not-content horizontal">

```html
<plus-popover header-text="Dismissable (Default)">
  <plus-button>Dismissable</plus-button>
</plus-popover>
<plus-popover header-text="Not Dismissable" dismissable=false>
  <plus-button>Not Dismissable</plus-button>
</plus-popover>
```

```tsx
import { PlusPopover, PlusButton } from '@plusui/library/react';

export default () => {
  return (
    <>
      <PlusPopover headerText="Dismissable (Default)">
        <PlusButton>Dismissable</PlusButton>
      </PlusPopover>
      <PlusPopover headerText="Not Dismissable" dismissable={false}>
        <PlusButton>Not Dismissable</PlusButton>
      </PlusPopover>
    </>
  );
};
```

```ts
import { Component } from '@angular/core';

@Component({
  selector: 'app-popover-dismissable',
  template: `
    <plus-popover header-text="Dismissable (Default)">
      <plus-button>Dismissable</plus-button>
    </plus-popover>
    <plus-popover header-text="Not Dismissable" [attr.dismissable]="false">
      <plus-button>Not Dismissable</plus-button>
    </plus-popover>
  `
})
export class PopoverDismissableComponent {}
```

```vue
<template>
  <plus-popover header-text="Dismissable (Default)">
    <plus-button>Dismissable</plus-button>
  </plus-popover>
  <plus-popover header-text="Not Dismissable" :dismissable="false">
    <plus-button>Not Dismissable</plus-button>
  </plus-popover>
</template>
```
</code-bubble>


## Visual Sections

### Layout & Spacing

<Image src={layoutSpacing} alt="Popover - layout spacing" />

### Light & Dark Mode

<Image src={lightDarkMode} alt="Popover - light & dark mode" />

## Accessibility (a11y)

*   **Keyboard Behavior:**
    *   When the popover is opened, focus is typically moved inside the popover panel, often to the first focusable element or the close button if available.
    *   `Tab` and `Shift+Tab` navigate between focusable elements within the popover. Focus is trapped within the popover when open.
    *   `Escape` closes the popover and returns focus to the trigger element.
*   **Screen Reader:**
    *   The component uses `role="dialog"`, `aria-labelledby` (pointing to the title element), and `aria-describedby` (pointing to the content element).
    *   `aria-hidden` is used to hide the popover content from screen readers when it is closed.
    *   When opened, screen readers announce the dialog's role, title, and description.
    *   The close button (if present) has an appropriate `aria-label`.
*   **Required Developer Actions:**
    *   Ensure the trigger element (in the default slot) is inherently focusable or has `tabindex="0"`.
    *   Provide clear and concise text for the `header-text` prop or `title` slot.
    *   Provide descriptive content via the `text` prop or `content` slot.
    *   If using custom content in slots, ensure it is accessible.
    *   Use appropriate `status` values if they convey semantic meaning beyond presentation.

## API Reference

### Props

| Name         | Type                                                                                             | Default     | Description                                                                | Required |
|--------------|--------------------------------------------------------------------------------------------------|-------------|----------------------------------------------------------------------------|----------|
| `size`       | `'sm' \| 'md' \| 'lg'`                                                                           | `'md'`      | Size of the popover panel.                                                 | No       |
| `orientation`| `'top' \| 'top-start' \| 'top-end' \| 'bottom' \| 'bottom-start' \| 'bottom-end' \| 'left' \| 'left-start' \| 'left-end' \| 'right' \| 'right-start' \| 'right-end'` | `'top'`     | Position relative to the trigger element.                                  | No       |
| `trigger`    | `'click' \| 'hover'`                                                                             | `'click'`   | How the popover is opened.                                                 | No       |
| `text`       | `string`                                                                                         | `undefined` | Main text content. Ignored if `content` slot is used.                        | No       |
| `headerText` | `string`                                                                                         | `'Title'`   | Header title text. Ignored if `title` slot is used.                        | No       |
| `status`     | `'success' \| 'warning' \| 'danger' \| 'info' \| 'primary' \| 'default'`                         | `'default'` | Color scheme and default icon based on status.                             | No       |
| `dismissable`| `boolean`                                                                                        | `true`      | Whether to show the default close button in the header.                   | No       |
| `statusIcon` | `boolean`                                                                                        | `true`      | Whether to show the default status icon in the header.                    | No       |
| `showArrow`  | `boolean`                                                                                        | `true`      | Whether to display the arrow pointing to the trigger element.              | No       |

### Events

| Name                   | Payload Type        | Description                                         |
|------------------------|---------------------|-----------------------------------------------------|
| `plus-popover-open`    | `CustomEvent<void>` | Fired when the popover opens.                     |
| `plus-popover-close`   | `CustomEvent<void>` | Fired when the popover closes (for any reason).     |
| `plus-popover-dismiss` | `CustomEvent<void>` | Fired specifically when closed via the close button. |

### Slots

| Name              | Description                                                     |
|-------------------|-----------------------------------------------------------------|
| `default` (unnamed) | The target element that triggers the popover.                   |
| `icon`            | Custom icon content in the header, replaces the default status icon. |
| `title`           | Custom title content in the header, overrides the `headerText` prop. |
| `actions`         | Content for the right side of the header (e.g., close button).    |
| `content`         | The main content inside the popover body. Overrides the `text` prop. |

### CSS Shadow Parts

| Part      | Description                          |
|-----------|--------------------------------------|
| `popover` | The main popover container element.  |
| `arrow`   | The arrow element pointing to the trigger. |
| `title`   | The title element container.         |
| `close`   | The close button area container.     |
| `content` | The main content area container.     |

