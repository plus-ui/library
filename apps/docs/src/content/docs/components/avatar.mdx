---
title: Avatar
description: Displays a user or entity image with a fallback option.
isComponent: true
since: 1.0.0
updated: 2025-07-01
version: 1.0.0
---

import { Tabs, TabItem, Aside } from '@astrojs/starlight/components';
import { CodePreview, EmbedContent } from '../../../component';
import { Image } from 'astro:assets';

import anatomy from '../../../assets/components/avatar/anatomy.png';
import layoutSpacing1 from '../../../assets/components/avatar/layout-spacing-1.png';
import layoutSpacing2 from '../../../assets/components/avatar/layout-spacing-2.png';
import layoutSpacing3 from '../../../assets/components/avatar/layout-spacing-3.png';
import lightDarkMode from '../../../assets/components/avatar/light-dark-mode.png';

The `Avatar` component displays an image representing a user or entity. It supports images, icons, or initials as fallback content when an image isn't available or fails to load.

## Anatomy

<Image src={anatomy} alt="Avatar - anatomy" />

## Usage

### Basic Example

<CodePreview autoGeneration={true} horizontal>
  <plus-avatar image="https://i.pravatar.cc/50" alt="User Name"></plus-avatar>
</CodePreview>

### Size

The `size` prop controls the dimensions of the avatar. It accepts predefined values (`xs`, `sm`, `md`, `lg`, `xl`) or any valid CSS size unit.

<CodePreview autoGeneration={true} horizontal>
  <plus-avatar data-variant-size="xs,sm,md,lg,xl"></plus-avatar>
</CodePreview>

### Shape

The `shape` prop determines the overall shape of the avatar.

<CodePreview autoGeneration={true} horizontal>
  <plus-avatar data-variant-shape="circle,square" image="https://i.pravatar.cc/50"></plus-avatar>
</CodePreview>

### Fallback Behavior

When an `image` is not provided or fails to load, the avatar displays fallback content based on the `fallbackStrategy` prop. You can provide `text` for initials or use the default `icon` (customizable via the `icon` prop).

<CodePreview autoGeneration={true} horizontal>
  <plus-avatar text="Murat Polat Özkan" fallback-strategy="text"></plus-avatar>
  <plus-avatar text="Single" fallback-strategy="text"></plus-avatar>
  <plus-avatar icon="ghost"></plus-avatar>
  <plus-avatar></plus-avatar>
</CodePreview>

## Visual Sections

### Layout & Spacing

Proper layout and spacing are crucial for integrating avatars seamlessly into the UI.

<Image src={layoutSpacing1} alt="Avatar - layout spacing 1" />
<Image src={layoutSpacing2} alt="Avatar - layout spacing 2" />
<Image src={layoutSpacing3} alt="Avatar - layout spacing 3" />

### Light & Dark Mode

The Avatar component adapts to both light and dark themes.

<Image src={lightDarkMode} alt="Avatar - light & dark mode" />

## Accessibility (a11y)

*   **Keyboard Behavior:** The component itself is not typically interactive, but if used within an interactive element (like a button or link), ensure that element follows standard keyboard navigation patterns.
*   **Screen Reader:** The `role="img"` is automatically applied. Provide meaningful alternative text via the `alt` prop, especially when an `image` is used. If `alt` is absent, the component attempts to use the `text` prop or a generic label, but explicit `alt` text is always recommended for clarity.
*   **Developer Actions:** Always provide a descriptive `alt` prop when using the `image` prop. Ensure sufficient color contrast between the avatar background and foreground (text/icon) if customizing colors.

## API Reference

### Properties

| Name             | Type                                                 | Default           | Description                                                                       |
|------------------|------------------------------------------------------|-------------------|-----------------------------------------------------------------------------------|
| `image`          | `string`                                             | `undefined`       | URL of the avatar image.                                                          |
| `alt`            | `string`                                             | `undefined`       | Alternative text for the image.                                                  |
| `shape`          | `'circle' \| 'square'`                               | `'circle'`        | Shape of the avatar.                                                              |
| `size`           | `'xs' \| 'sm' \| 'md' \| 'lg' \| 'xl' \| string` | `'md'`            | Size of the avatar (predefined or custom CSS unit).                               |
| `icon`           | `string`                                             | `'fa-solid fa-user'` | CSS class for the fallback icon.                                                  |
| `invert`         | `boolean`                                            | `false`           | Toggles the inverted color scheme.                                              |
| `text`           | `string`                                             | `undefined`       | Text to display as initials.                                                      |
| `fallbackStrategy` | `'icon' \| 'text' \| 'custom'`                    | `'icon'`          | Strategy when image fails to load.                                                 |

### Events

| Name    | Payload Type        | Description                        |
|---------|---------------------|------------------------------------|
| `error` | `CustomEvent<void>` | Fired when the image fails to load. |
| `load`  | `CustomEvent<void>` | Fired when the image loads.        |

### Slots

| Name        | Description                                                   |
|-------------|---------------------------------------------------------------|
| `(default)` | Main content area (image, icon, or text).                     |
| `fallback`  | Custom content for `fallbackStrategy='custom'`.                |

### CSS Custom Properties

| Name           | Description                           |
|----------------|---------------------------------------|
| `--text-color` | Text color for initials or icon.      |
| `--bg-default` | Default background color.             |

### Shadow Parts

| Name   | Description                     |
|--------|---------------------------------|
| `base` | The main container element.      |
