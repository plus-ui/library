---
title: Button Group
description: Displays a set of related buttons in a horizontal or vertical arrangement.
---
import { Tabs, TabItem, Aside } from '@astrojs/starlight/components';
import EmbedContent from '../../../components/EmbedContent.astro';
import { Image } from 'astro:assets';

import anatomy from '../../../assets/components/button-group/anatomy.png';
import layoutSpacing from '../../../assets/components/button-group/layout-spacing.png';
import lightDarkMode from '../../../assets/components/button-group/light-dark-mode.png';

The Button Group component organizes multiple related buttons into a single cohesive unit, providing a clear visual relationship between similar actions. This component is useful for creating segmented controls, toolbars, and pagination interfaces.

## Anatomy

<Image src={anatomy} alt="Button Group - anatomy" />

## Usage

### Basic Example

Create a button group by wrapping multiple `plus-button` components within a `plus-button-group`.

<code-bubble class="not-content horizontal">
```html
<plus-button-group>
  <plus-button kind="filled" status="primary">Left</plus-button>
  <plus-button kind="filled" status="primary">Middle</plus-button>
  <plus-button kind="filled" status="primary">Right</plus-button>
</plus-button-group>
```

```tsx
import { PlusButtonGroup, PlusButton } from '@plusui/library/react';

export default () => {
  return (
    <PlusButtonGroup>
      <PlusButton kind="filled" status="primary">Left</PlusButton>
      <PlusButton kind="filled" status="primary">Middle</PlusButton>
      <PlusButton kind="filled" status="primary">Right</PlusButton>
    </PlusButtonGroup>
  );
};
```

```ts
import { Component } from '@angular/core';

@Component({
  selector: 'app-button-group-basic',
  template: `
    <plus-button-group>
      <plus-button kind="filled" status="primary">Left</plus-button>
      <plus-button kind="filled" status="primary">Middle</plus-button>
      <plus-button kind="filled" status="primary">Right</plus-button>
    </plus-button-group>
  `
})
export class ButtonGroupBasicComponent {}
```

```vue
<template>
  <plus-button-group>
    <plus-button kind="filled" status="primary">Left</plus-button>
    <plus-button kind="filled" status="primary">Middle</plus-button>
    <plus-button kind="filled" status="primary">Right</plus-button>
  </plus-button-group>
</template>
```
</code-bubble>

### Override Properties

Use the `override` property to make all buttons in the group inherit the properties defined on the button group. This ensures visual consistency between all buttons.

<code-bubble class="not-content horizontal">
```html
<!-- With override (default) -->
<plus-button-group override kind="outlined" status="success" size="md">
  <plus-button>Save</plus-button>
  <plus-button>Cancel</plus-button>
  <plus-button>Reset</plus-button>
</plus-button-group>

<!-- Without override -->
<plus-button-group override="false">
  <plus-button kind="filled" status="primary">Primary</plus-button>
  <plus-button kind="outlined" status="danger">Danger</plus-button>
  <plus-button kind="dashed" status="warning">Warning</plus-button>
</plus-button-group>
```

```tsx
import { PlusButtonGroup, PlusButton } from '@plusui/library/react';

export default () => {
  return (
    <>
      {/* With override (default) */}
      <PlusButtonGroup override kind="outlined" status="success" size="md">
        <PlusButton>Save</PlusButton>
        <PlusButton>Cancel</PlusButton>
        <PlusButton>Reset</PlusButton>
      </PlusButtonGroup>

      <br />
      <br />

      {/* Without override */}
      <PlusButtonGroup override={false}>
        <PlusButton kind="filled" status="primary">Primary</PlusButton>
        <PlusButton kind="outlined" status="danger">Danger</PlusButton>
        <PlusButton kind="dashed" status="warning">Warning</PlusButton>
      </PlusButtonGroup>
    </>
  );
};
```

```ts
import { Component } from '@angular/core';

@Component({
  selector: 'app-button-group-override',
  template: `
    <!-- With override (default) -->
    <plus-button-group override kind="outlined" status="success" size="md">
      <plus-button>Save</plus-button>
      <plus-button>Cancel</plus-button>
      <plus-button>Reset</plus-button>
    </plus-button-group>

    <br />
    <br />

    <!-- Without override -->
    <plus-button-group override="false">
      <plus-button kind="filled" status="primary">Primary</plus-button>
      <plus-button kind="outlined" status="danger">Danger</plus-button>
      <plus-button kind="dashed" status="warning">Warning</plus-button>
    </plus-button-group>
  `
})
export class ButtonGroupOverrideComponent {}
```

```vue
<template>
  <!-- With override (default) -->
  <plus-button-group override kind="outlined" status="success" size="md">
    <plus-button>Save</plus-button>
    <plus-button>Cancel</plus-button>
    <plus-button>Reset</plus-button>
  </plus-button-group>

  <br />
  <br />

  <!-- Without override -->
  <plus-button-group :override="false">
    <plus-button kind="filled" status="primary">Primary</plus-button>
    <plus-button kind="outlined" status="danger">Danger</plus-button>
    <plus-button kind="dashed" status="warning">Warning</plus-button>
  </plus-button-group>
</template>
```
</code-bubble>

### Orientation

Use the `orientation` property to arrange buttons horizontally (default) or vertically.

<code-bubble class="not-content horizontal">
```html
<!-- Horizontal button group (default) -->
<plus-button-group>
  <plus-button kind="filled" status="primary">Day</plus-button>
  <plus-button kind="filled" status="primary">Week</plus-button>
  <plus-button kind="filled" status="primary">Month</plus-button>
</plus-button-group>

<!-- Vertical button group -->
<plus-button-group orientation="vertical">
  <plus-button kind="filled" status="primary">Top</plus-button>
  <plus-button kind="filled" status="primary">Middle</plus-button>
  <plus-button kind="filled" status="primary">Bottom</plus-button>
</plus-button-group>
```

```tsx
import { PlusButtonGroup, PlusButton } from '@plusui/library/react';

export default () => {
  return (
    <>
      {/* Horizontal button group (default) */}
      <PlusButtonGroup>
        <PlusButton kind="filled" status="primary">Day</PlusButton>
        <PlusButton kind="filled" status="primary">Week</PlusButton>
        <PlusButton kind="filled" status="primary">Month</PlusButton>
      </PlusButtonGroup>

      <br />
      <br />

      {/* Vertical button group */}
      <PlusButtonGroup orientation="vertical">
        <PlusButton kind="filled" status="primary">Top</PlusButton>
        <PlusButton kind="filled" status="primary">Middle</PlusButton>
        <PlusButton kind="filled" status="primary">Bottom</PlusButton>
      </PlusButtonGroup>
    </>
  );
};
```

```ts
import { Component } from '@angular/core';

@Component({
  selector: 'app-button-group-orientation',
  template: `
    <!-- Horizontal button group (default) -->
    <plus-button-group>
      <plus-button kind="filled" status="primary">Day</plus-button>
      <plus-button kind="filled" status="primary">Week</plus-button>
      <plus-button kind="filled" status="primary">Month</plus-button>
    </plus-button-group>

    <br />
    <br />

    <!-- Vertical button group -->
    <plus-button-group orientation="vertical">
      <plus-button kind="filled" status="primary">Top</plus-button>
      <plus-button kind="filled" status="primary">Middle</plus-button>
      <plus-button kind="filled" status="primary">Bottom</plus-button>
    </plus-button-group>
  `
})
export class ButtonGroupOrientationComponent {}
```

```vue
<template>
  <!-- Horizontal button group (default) -->
  <plus-button-group>
    <plus-button kind="filled" status="primary">Day</plus-button>
    <plus-button kind="filled" status="primary">Week</plus-button>
    <plus-button kind="filled" status="primary">Month</plus-button>
  </plus-button-group>

  <br />
  <br />

  <!-- Vertical button group -->
  <plus-button-group orientation="vertical">
    <plus-button kind="filled" status="primary">Top</plus-button>
    <plus-button kind="filled" status="primary">Middle</plus-button>
    <plus-button kind="filled" status="primary">Bottom</plus-button>
  </plus-button-group>
</template>
```
</code-bubble>

### Button Styles (kind and status)

Button group can control the visual style and color of all its buttons when using the `override` property.

<code-bubble class="not-content horizontal">
```html
<!-- Outlined style -->
<plus-button-group kind="outlined" status="primary">
  <plus-button>Cut</plus-button>
  <plus-button>Copy</plus-button>
  <plus-button>Paste</plus-button>
</plus-button-group>

<!-- Dashed style -->
<plus-button-group kind="dashed" status="warning">
  <plus-button>Edit</plus-button>
  <plus-button>Delete</plus-button>
</plus-button-group>

<!-- Text style -->
<plus-button-group kind="text" status="default">
  <plus-button>Previous</plus-button>
  <plus-button>Next</plus-button>
</plus-button-group>
```

```tsx
import { PlusButtonGroup, PlusButton } from '@plusui/library/react';

export default () => {
  return (
    <>
      {/* Outlined style */}
      <PlusButtonGroup kind="outlined" status="primary">
        <PlusButton>Cut</PlusButton>
        <PlusButton>Copy</PlusButton>
        <PlusButton>Paste</PlusButton>
      </PlusButtonGroup>

      <br />
      <br />

      {/* Dashed style */}
      <PlusButtonGroup kind="dashed" status="warning">
        <PlusButton>Edit</PlusButton>
        <PlusButton>Delete</PlusButton>
      </PlusButtonGroup>

      <br />
      <br />

      {/* Text style */}
      <PlusButtonGroup kind="text" status="default">
        <PlusButton>Previous</PlusButton>
        <PlusButton>Next</PlusButton>
      </PlusButtonGroup>
    </>
  );
};
```

```ts
import { Component } from '@angular/core';

@Component({
  selector: 'app-button-group-styles',
  template: `
    <!-- Outlined style -->
    <plus-button-group kind="outlined" status="primary">
      <plus-button>Cut</plus-button>
      <plus-button>Copy</plus-button>
      <plus-button>Paste</plus-button>
    </plus-button-group>

    <br />
    <br />

    <!-- Dashed style -->
    <plus-button-group kind="dashed" status="warning">
      <plus-button>Edit</plus-button>
      <plus-button>Delete</plus-button>
    </plus-button-group>

    <br />
    <br />

    <!-- Text style -->
    <plus-button-group kind="text" status="default">
      <plus-button>Previous</plus-button>
      <plus-button>Next</plus-button>
    </plus-button-group>
  `
})
export class ButtonGroupStylesComponent {}
```

```vue
<template>
  <!-- Outlined style -->
  <plus-button-group kind="outlined" status="primary">
    <plus-button>Cut</plus-button>
    <plus-button>Copy</plus-button>
    <plus-button>Paste</plus-button>
  </plus-button-group>

  <br />
  <br />

  <!-- Dashed style -->
  <plus-button-group kind="dashed" status="warning">
    <plus-button>Edit</plus-button>
    <plus-button>Delete</plus-button>
  </plus-button-group>

  <br />
  <br />

  <!-- Text style -->
  <plus-button-group kind="text" status="default">
    <plus-button>Previous</plus-button>
    <plus-button>Next</plus-button>
  </plus-button-group>
</template>
```
</code-bubble>

### Size

Control the size of all buttons within the group using the `size` property. This ensures consistent sizing across the entire group.

<code-bubble class="not-content horizontal">
```html
<plus-button-group size="sm" override>
  <plus-button>Small</plus-button>
  <plus-button>Buttons</plus-button>
</plus-button-group>

<plus-button-group size="md" override>
  <plus-button>Medium</plus-button>
  <plus-button>Buttons</plus-button>
</plus-button-group>

<plus-button-group size="lg" override>
  <plus-button>Large</plus-button>
  <plus-button>Buttons</plus-button>
</plus-button-group>
```

```tsx
import { PlusButtonGroup, PlusButton } from '@plusui/library/react';

export default () => {
  return (
    <>
      <PlusButtonGroup size="sm" override>
        <PlusButton>Small</PlusButton>
        <PlusButton>Buttons</PlusButton>
      </PlusButtonGroup>

      <br />
      <br />

      <PlusButtonGroup size="md" override>
        <PlusButton>Medium</PlusButton>
        <PlusButton>Buttons</PlusButton>
      </PlusButtonGroup>

      <br />
      <br />

      <PlusButtonGroup size="lg" override>
        <PlusButton>Large</PlusButton>
        <PlusButton>Buttons</PlusButton>
      </PlusButtonGroup>
    </>
  );
};
```

```ts
import { Component } from '@angular/core';

@Component({
  selector: 'app-button-group-size',
  template: `
    <plus-button-group size="sm" override>
      <plus-button>Small</plus-button>
      <plus-button>Buttons</plus-button>
    </plus-button-group>

    <br />
    <br />

    <plus-button-group size="md" override>
      <plus-button>Medium</plus-button>
      <plus-button>Buttons</plus-button>
    </plus-button-group>

    <br />
    <br />

    <plus-button-group size="lg" override>
      <plus-button>Large</plus-button>
      <plus-button>Buttons</plus-button>
    </plus-button-group>
  `
})
export class ButtonGroupSizeComponent {}
```

```vue
<template>
  <plus-button-group size="sm" override>
    <plus-button>Small</plus-button>
    <plus-button>Buttons</plus-button>
  </plus-button-group>

  <br />
  <br />

  <plus-button-group size="md" override>
    <plus-button>Medium</plus-button>
    <plus-button>Buttons</plus-button>
  </plus-button-group>

  <br />
  <br />

  <plus-button-group size="lg" override>
    <plus-button>Large</plus-button>
    <plus-button>Buttons</plus-button>
  </plus-button-group>
</template>
```
</code-bubble>

### Disabled and Loading State

Control the state of all buttons in the group with the `disabled` and `loading` properties.

<code-bubble class="not-content horizontal">
```html
<!-- Disabled button group -->
<plus-button-group disabled override>
  <plus-button>Disabled</plus-button>
  <plus-button>Buttons</plus-button>
</plus-button-group>

<!-- Loading button group -->
<plus-button-group loading override>
  <plus-button>Loading</plus-button>
  <plus-button>Buttons</plus-button>
</plus-button-group>
```

```tsx
import { PlusButtonGroup, PlusButton } from '@plusui/library/react';

export default () => {
  return (
    <>
      {/* Disabled button group */}
      <PlusButtonGroup disabled override>
        <PlusButton>Disabled</PlusButton>
        <PlusButton>Buttons</PlusButton>
      </PlusButtonGroup>

      <br />
      <br />

      {/* Loading button group */}
      <PlusButtonGroup loading override>
        <PlusButton>Loading</PlusButton>
        <PlusButton>Buttons</PlusButton>
      </PlusButtonGroup>
    </>
  );
};
```

```ts
import { Component } from '@angular/core';

@Component({
  selector: 'app-button-group-state',
  template: `
    <!-- Disabled button group -->
    <plus-button-group disabled override>
      <plus-button>Disabled</plus-button>
      <plus-button>Buttons</plus-button>
    </plus-button-group>

    <br />
    <br />

    <!-- Loading button group -->
    <plus-button-group loading override>
      <plus-button>Loading</plus-button>
      <plus-button>Buttons</plus-button>
    </plus-button-group>
  `
})
export class ButtonGroupStateComponent {}
```

```vue
<template>
  <!-- Disabled button group -->
  <plus-button-group disabled override>
    <plus-button>Disabled</plus-button>
    <plus-button>Buttons</plus-button>
  </plus-button-group>

  <br />
  <br />

  <!-- Loading button group -->
  <plus-button-group loading override>
    <plus-button>Loading</plus-button>
    <plus-button>Buttons</plus-button>
  </plus-button-group>
</template>
```
</code-bubble>

## Visual Sections

### Layout & Spacing

<Image src={layoutSpacing} alt="Button Group - layout spacing" />

### Light & Dark Mode

<Image src={lightDarkMode} alt="Button Group - light & dark mode" />

## Accessibility (a11y)

*   **Keyboard Behavior:**
    *   Users can navigate between buttons using the `Tab` key.
    *   Each button can be activated using `Enter` or `Space`.
*   **Screen Reader:**
    *   The button group has `role="group"` to indicate to assistive technologies that these buttons are related.
*   **Required Developer Actions:**
    *   Ensure that button text is descriptive and clearly indicates its action.
    *   Consider adding an additional `aria-label` attribute to the button group if the purpose of the group is not clear from button text alone.

## API Reference

### Properties

| Name          | Type                                                              | Default      | Description                                                           | Required |
|---------------|-------------------------------------------------------------------|--------------|-----------------------------------------------------------------------|----------|
| `orientation` | `'horizontal' \| 'vertical'`                                       | `'horizontal'` | The orientation of the button group.                                  | No       |
| `override`    | `boolean`                                                         | `true`       | When true, button group properties override child button properties.  | No       |
| `kind`        | `'filled' \| 'outlined' \| 'dashed' \| 'text'`                     | `'filled'`   | The visual style of buttons when override is true.                    | No       |
| `status`      | `'default' \| 'primary' \| 'success' \| 'warning' \| 'danger' \| 'info'` | `'default'`  | The color variant of buttons when override is true.                   | No       |
| `size`        | `'sm' \| 'md' \| 'lg'`                                             | `'md'`       | Size of all buttons in the group when override is true.               | No       |
| `disabled`    | `boolean`                                                         | `false`      | Disables all buttons in the group when override is true.              | No       |
| `loading`     | `boolean`                                                         | `false`      | Shows loading state for all buttons when override is true.            | No       |

### Slots

| Name      | Description                           |
|-----------|---------------------------------------|
| (default) | Container for button elements.        |

### CSS Shadow Parts

| Part         | Description                        |
|--------------|------------------------------------|
| `base`       | The component's base wrapper.      |

### CSS Custom Properties

| Name                     | Description                      |
|--------------------------|----------------------------------|
| `--button-group-gap`     | Gap between buttons in the group (default is 0). |

