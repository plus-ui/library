import { html } from 'lit';
import { property } from 'lit/decorators.js';
import { Tailwind } from '../base/index.js';
import styles from './{{kebabCase name}}.styles.js';

/**
 * Add a description here
 *
 * @tag {{kebabCase tagName}}
 * @since 0.0.0
 * @status experimental
 *
 **/
export class {{pascalCase tagName}} extends Tailwind {
  static override styles = styles;

  @property() 
  heading = 'Hello, word!';

  override render() {
    return html`
      <h1>${this.heading}</h1>
    `;
  }
}

export default {{pascalCase tagName}};
